[project]
name = "helios-sdl"
version = "0.1.0"
description = "HELIOS — agent-native orchestrator for self-driving laboratories: typed contracts, specialist agent swarms, safety gates, durable campaigns, and research-grade Bayesian optimization"
readme = "README.md"
license = {text = "MIT"}
requires-python = ">=3.11"
dependencies = [
  # Web framework
  "fastapi>=0.115.0",
  "uvicorn[standard]>=0.30.0",
  "pydantic>=2.8.0",
  "pyyaml>=6.0",
  "httpx>=0.27.0",
  # Numeric / RL
  "numpy>=1.24.0",
]

[project.optional-dependencies]
# Real hardware communication
hardware = [
  "pyserial>=3.5",
  "opentrons>=7.0.0",
]
# DQN / neural RL strategies
ml = [
  "torch>=2.0.0",
]
# Gradio frontend for NLP agent
frontend = [
  "gradio>=4.0.0",
]
# Demo scripts
demo = [
  "requests>=2.28.0",
]
# Development & testing
dev = [
  "pytest>=8.3.0",
  "pytest-asyncio>=0.23.0",
  "pytest-cov>=5.0.0",
  "httpx>=0.27.0",
  "anyio>=4.0.0",
  "ruff>=0.4.0",
  "mypy>=1.10.0",
]
# Install everything
all = [
  "helios-sdl[hardware,ml,frontend,demo,dev]",
]

[tool.setuptools.packages.find]
include = ["app*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["."]
asyncio_mode = "auto"

[tool.ruff]
line-length = 120
target-version = "py311"

[tool.ruff.lint]
select = ["E", "F", "I", "W", "UP", "B"]
ignore = ["E501"]

[tool.ruff.lint.per-file-ignores]
"scripts/*.py" = ["E402"]

[tool.mypy]
python_version = "3.11"
warn_return_any = true
warn_unused_configs = true
ignore_missing_imports = true
